{
  "name": "counterask",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A menswear storefront whose WebMCP tools return a question when answering would be a guess.",
  "license": "MIT",
  "scripts": {
    "start": "python -m http.server 5173 --directory public",
    "test": "node scripts/parse_test.mjs && node scripts/tools_test.mjs && node scripts/bench.mjs 400 && node scripts/agentbench.mjs 400 --holdout",
    "bench": "node scripts/bench.mjs 800 && node scripts/agentbench.mjs 800 && node scripts/agentbench.mjs 800 --holdout",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
